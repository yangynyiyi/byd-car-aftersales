server:
  # 后端服务端口
  port: 8080

spring:
  application:
    # 应用名称
    name: byd-aftersales
  datasource:
    # MySQL 数据库连接配置，运行前请根据本机环境修改用户名和密码
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/byd_aftersales?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Liyuchen@050628
  sql:
    init:
      # 建议手动执行 db/schema.sql 和 db/sample-data.sql，避免重复初始化
      mode: never
