server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/byd_aftersale?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    database: 0

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

# 远程 Agent 配置（阿里云百炼 - 通义千问）
ai:
  agent:
    api-key: sk-0b7ca4f53cee418e9ae05516daec2c9d
    base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
    model: qwen-plus
    timeout: 30000

logging:
  level:
    com.byd: debug
